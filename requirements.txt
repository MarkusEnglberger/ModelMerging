# install torch separately with the cu121 index:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
transformers==4.44.2
datasets==2.21.0
evaluate==0.4.3
scikit-learn
scipy
pillow  # CLIP/ViT vision track: image decoding for CLIPImageProcessor
pyyaml
accelerate==0.34.2
pandas
# MaTS/T0 P3 benchmark templates (optional unless modality=t5_mats).
promptsource==0.2.3
# IFEval strict scoring on the MergeBench decoder track (src/apr/ifeval).
# nltk also needs punkt/punkt_tab data; NLTK_DATA is set in slurm/common.sh.
langdetect
immutabledict
nltk
